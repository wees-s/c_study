#include <stdio.h> // Necessário importar uma biblioteca, no caso, a stdio.h. Ela é responsável por fornecer funções de entrada e saída padrão.
//#include + nome da biblioteca entre <>, indica que a biblioteca está no diretório padrão do compilador.
int main() {//início da função, tudo que estiver entre {} será executado.
    printf("Hello World!");//printf faz a print padrão no terminal.
    return 0; //indica o fim da função
}